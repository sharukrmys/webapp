spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://dev-db-host:5432/master_db
    username: postgres
    password: postgres

# Development environment specific settings
aws:
  enabled: true
  region: us-east-1
  sqs:
    enabled: true
  s3:
    enabled: true
  secretsmanager:
    enabled: true
  
redis:
  enabled: true
  host: dev-redis-host
  port: 6379
  
kafka:
  enabled: true
  bootstrap-servers: dev-kafka-host:9092

