spring:
  application:
    name: app-lib-test
  cloud:
    openfeign:
      client:
        config:
          default:
            connectTimeout: 1000
            readTimeout: 1000
            loggerLevel: full

logging:
  level:
    com.example: DEBUG
    org.springframework: INFO

